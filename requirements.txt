# Root requirements.txt
# Use this to install dependencies for all services for local development.
# For production, each service should use its own requirements.txt.

-r geo_api/requirements.txt
-r llm_api/requirements.txt
-r map_api/requirements.txt
-r parser_api/requirements.txt
-r orchestrator/requirements.txt

# Add any shared development tools below (example, can be uncommented if needed):
# pytest
# pylint
# black
# mypy
